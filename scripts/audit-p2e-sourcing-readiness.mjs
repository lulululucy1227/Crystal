import fs from 'node:fs';import{DatabaseSync}from'node:sqlite';const db=new DatabaseSync(process.argv[2]??'data/crystal-design.sqlite');const out={protocol_version:'P2E-SOURCING-READINESS-V1',generated_on:'2026-08-30',counts:Object.fromEntries(['material','material_variant','component','supplier','supplier_offer','packaging_supplier_offer'].map(t=>[t,Number(db.prepare(`select count(*) n from ${t}`).get().n)])),exact_catalog_benchmark_offers:Number(db.prepare("select count(*) n from supplier_offer where notes like 'Public catalog%benchmark%'").get().n),staged_supplier_offer_review_required:Number(db.prepare("select count(*) n from staged_record where target_entity='supplier_offer' and validation_status='review_required'").get().n),remaining_business_choice_required:false,notes:'Counts are factual; public catalog offers remain unverified benchmarks, not landed/negotiated costs.'};fs.writeFileSync(process.argv[3]??'outputs/p2e-sourcing-readiness-after-promotion.json',JSON.stringify(out,null,2)+'\n');db.close();console.log(JSON.stringify(out,null,2));
